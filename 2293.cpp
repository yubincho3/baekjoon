#include <bits/stdc++.h>
using namespace std;

int dp[100001];
int arr[101];
int n, k;

int main(void)
{
	scanf("%d %d", &n, &k);
	for (int i = 0; i < n; i++)
		scanf("%d", &arr[i]);
	
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j + dp[k])
	}
	
	return 0;
}

